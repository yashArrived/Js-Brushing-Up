// In
/*
class React{
constructor() {
    this.library = "React"
    this.server = "https://localhost:300"

    //requirement
    document
        .querySelector('button')
        .addEventListener('click', this.handleClick.bind(this))
}

handleClick() {
    console.log("button clicked");
    console.log(this.server);
}
}

When we used bind in handleclick call , we bind all the methods and elements 
from the object or class and will send all those to the fucntion declaration.





*/