"use strict" //treat all js code as newer version

// alert(3+3);  //We are using nodejs not browser

//Code readibility should always be high.

//ECMA defines the standards of writing JS

//DATATYPES

//Primitive
let name = "Yash"; //string
let age = 18        //number => 2^53
let isLoggedIn = false;  //boolean
                        //null -> it is datatype and a standalone value: definition of empty value
                        // for example temperature = null;
                        //undefined -> declared but not defined
//Symbols -> uniqueness define

//Objects
console.log(typeof(null))  // -->Object
console.log(typeof(undefined)) //undefined
